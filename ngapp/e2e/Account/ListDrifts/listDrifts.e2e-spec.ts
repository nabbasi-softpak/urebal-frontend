/*
import {DriftPage} from "./Drifts.po";
import {browser, ExpectedConditions} from "protractor";

describe('List drifts page navigation', function () {
  let driftList:DriftPage;
  beforeEach(() => {
    driftList = new DriftPage();

  });
  it('Navigate to drift list by URL', () => {
    driftList.navigateToDriftList();
    expect(browser.getCurrentUrl()).toContain('drift/list');


  })

})
*/
